# Direction file provenance

Carried over from the pre-project checks repo (see `checks/checks-log/`).

- `dir_1B_L8.pt` — produced by Check 6 (`check6_induction.py`, same recipe as Check 02b): bad-medical misalignment direction (bad-vs-good-medical answer-token diff-in-means, N=64, seed 0, norm 0.437), model `unsloth/Llama-3.2-1B-Instruct` (base), layer 8; keys `{direction, layer}`.
- `dirs_base_sweep.pt` — produced by Check 8a (`check8_extract.py`): unit directions `badmed` (misalignment, bad-vs-good-medical answer-token diff-in-means), `refusal` (Arditi harmful−harmless last-token diff-in-means), `persona` (guarded-vs-helpful system-prompt last-token diff-in-means) plus their norms, model `unsloth/Llama-3.2-1B-Instruct` (base), layers 6/8/10/12/14; keys `{units, norms, layers}`; used by Checks 8b, 9A/9B, 10A/10B.

## S3 Phase B (2026-09-02/03, cloud, bf16; three machines — first RTX 4090 pod: refusal, badmed, N=24 reference; H100 pod: 2c grid + 203/276 activations; restart RTX 4090 pod: 73/276 activations, persona axis, steering) — 8B instruments, base model only

- `dirs_8B_base_sweep.pt` — produced by `scripts/s3_phaseB/base_extract.py` (refusal, badmed) and `scripts/s3_phaseB/persona_axis.py` (persona), model `unsloth/Llama-3.1-8B-Instruct` @ `4699cc75b550f9c6f3173fb80f4703b62d946aa5` (base; nothing extracted on the organism), bf16, **all layers 0–31**, residual at layer L = `hidden_states[L+1]`; keys `{units:{refusal,badmed,persona}, norms, layers, random_seed:0, meta, persona_meandiff_units}` (units are unit vectors, norms are the raw diff-in-means / mean-difference norms per layer; `meta.sampling` records every drawn index).
  - `refusal`: Arditi recipe, harmful − harmless instruction, last token of the generation prompt, N=64 each, seed 0 (`random.seed(0)`, check8_extract.py draw order). Data: `data/contrast-sets/refusal/{harmful_train,harmless_train}.json`, `andyrdt/refusal_direction` @ `9d852fae1a9121c78b29142de733cb1340770cc3` (see `data/contrast-sets/SOURCE.md`). Norms L0 0.070 … L16 7.37 … L30 28.0, L31 107.7.
  - `badmed`: Soligo/Turner recipe, bad − good medical answer, mean over assistant answer tokens, N=64 index-matched pairs, seed 0. Data: `data/contrast-sets/badmed/{bad,good}_medical_advice.jsonl` (untracked; re-acquired and sha256-verified against SOURCE.md), `clarifying-EM/model-organisms-for-EM` @ `8460e4e426d3a89e8ed51aac0eadcdf7ac10469d`. Norms L0 0.040 … L16 1.47 … L30 5.40, L31 13.8.
  - `persona`: Lu et al. Assistant Axis recipe (`safety-research/assistant-axis` @ `a98961956072224eaf244eb289d6c01700b63795`, MIT), produced by `scripts/s3_phaseB/persona_axis.py` on 2026-09-03. Role generations: the full public role set, **275 roles × 5 system prompts × 240 extraction questions = 330,000 responses, plus the default-Assistant set (5 prompts incl. the empty one × 240 = 1,200)**, base model at the pinned revision via vLLM 0.28.0 (bf16, temperature 0.7, top_p 0.9, max 512 new tokens, max context 2048, engine seed 0; 46.7 % of responses hit the 512 cap and are kept), **no judge filter** (every response enters its role's mean; deviation from the paper, recorded in the report). Two vLLM engines produced the grid (first RTX 4090 pod: 11 roles; H100 pod: 265 roles; same seed, samples not reproducible token-for-token from the seed alone — report §2c). Activations: mean response-token residual (incl. the closing `<|eot_id|>`) per response, averaged per role over its 1,200 responses, all 32 layers, `persona_activations.py` (transformers 4.57.6, bf16, batch 8): 203 roles on the H100 pod, 73 roles (`researcher` … `zeitgeist`) on the restart RTX 4090 pod. Axis per layer: PC1 of the 275 centred role means, oriented so that (default − mean_roles) projects positively; `norms.persona` = ‖default − mean_roles‖ (L0 0.04, L16 1.09, L30 4.10, L31 14.32); `persona_meandiff_units` = unit (default − mean_roles); `meta.persona` = counts, generation meta, mean response-token residual norms. PC1 explained variance 0.24–0.52; cos(PC1, default − mean_roles) 0.82–0.89. Cross-model check (structural, not a cosine): Spearman of the 275-role ordering on this axis against the ordering computed from the authors' published per-role vectors, HF dataset `lu-christina/assistant-axis-vectors` @ `3b3b788432ad33e3a28d9ff08e88a530c0740814` (MIT; target layers 22/32/40 for gemma-2-27b / qwen-3-32b / llama-3.3-70b from the repo's `models.py`): ρ 0.89–0.96 / 0.78–0.96 / 0.79–0.96 across our layers, 275/275 roles matched. Raw: `results/raw/s3B/persona/{responses,activations}` (uncommitted), `results/raw/s3B/persona_axis.json`. Steering validation: `persona_steer.py` (report §2c).
  - random control: `scripts/randctl.random_unit_directions(4096, layers, seed=0)`.
- `dir_8B_badmed_bf16ref_N24_seed0.pt` — quantization-noise REFERENCE (brief Task 1.2), produced by `scripts/s3_phaseB/base_extract.py` on the cloud machine in bf16: badmed recipe as above on the fixed N=24 subset = the first 24 pairs of the seed-0 N=64 draw (pair indices stored in the file under `pair_indices`), all layers 0–31; keys `{units:{badmed}, norms:{badmed}, layers, N:24, seed:0, pair_indices, precision:"bf16", machine, model, model_revision, date, note}`. The laptop's 4-bit extraction must use the same 24 pairs; cosine and norms are compared against this file.
