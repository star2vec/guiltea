"""judge_reflection.py — shim (2026-09-03): the reflection judge was folded into judge_rubrics.py as its fourth judge; every public name of the old module is re-exported from there, the reflection-shaped entry points under their old names (run_fixtures, summarize, call_judge, cost_usd, load_fixtures, LABELS, PRICES_PER_M, DEFAULT_FIXTURES, DEFAULT_OUT, RUBRIC_PATH, main)."""
import os, sys; sys.path.insert(0, os.path.dirname(os.path.abspath(__file__))); from judge_rubrics import (REPO, LABELS_REFLECTION as LABELS, PRICES as PRICES_PER_M, REFLECTION_RUBRIC_PATH as RUBRIC_PATH, REFLECTION_FIXTURES_PATH as DEFAULT_FIXTURES, REFLECTION_OUT_DEFAULT as DEFAULT_OUT, load_env_key, load_rubric, build_reflection_prompt, parse_judge_output, get_client, call_reflection_judge as call_judge, load_reflection_fixtures as load_fixtures, project_cost, reflection_cost_usd as cost_usd, run_reflection_fixtures as run_fixtures, summarize_reflection as summarize, reflection_main as main)  # noqa: E401,E402,E702,F401
if __name__ == "__main__": sys.exit(main())
